<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alterar Foto de Perfil</title>
    <style>
        /* Estiliza o ícone de perfil */
        .profile-pic-container {
            position: relative;
            width: 100px;
            height: 100px;
            border-radius: 50%;
            overflow: hidden;
            cursor: pointer;
        }
        .profile-pic-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        /* Botão de upload invisível */
        input[type="file"] {
            display: none;
        }
        /* Ícone de edição no canto inferior direito */
        .edit-icon {
            position: absolute;
            bottom: 0;
            right: 0;
            background-color: #000;
            color: #fff;
            border-radius: 50%;
            padding: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h2>Alterar Foto de Perfil</h2>

    <!-- Container da imagem do perfil -->
    <form id="upload-form" enctype="multipart/form-data" method="POST" action="/upload-foto">
        <label class="profile-pic-container">
            <!-- Imagem do perfil -->
            <img src="https://mdbcdn.b-cdn.net/img/new/avatars/2.webp" class="rounded-circle" style="width: 150px;"
  alt="Avatar" />
            <!-- Ícone de edição (opcional) -->
            <span class="edit-icon">✏️</span>
            <!-- Input de arquivo escondido -->
            <input type="file" name="fotoPerfil" accept="image/*" onchange="this.form.submit()" />
        </label>
    </form>

</body>
</html>
